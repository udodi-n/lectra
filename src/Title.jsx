function Title({title}) {
            return(
                <h2 className="font-[Google_Sans_Flex] underline text-3xl text-white">
                {title}
                </h2>
            )

}

export default Title